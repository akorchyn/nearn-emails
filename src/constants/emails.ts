import { PROJECT_NAME } from './project';

export const ceoEmail = `${PROJECT_NAME} Team <${process.env.CEO_EMAIL}>`;
export const listingsEmail = `${PROJECT_NAME} Team <${process.env.LISTINGS_EMAIL}>`;
export const alertsEmail = `${PROJECT_NAME} Team <${process.env.ALERTS_EMAIL}>`;
